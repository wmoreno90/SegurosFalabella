@model IEnumerable<Falabella.Models.AseguradorasViewModel>

@{
    ViewBag.Title = "Compañías aseguradoras aliadas";
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("Inicio", "Inicio")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.ActionLink("Editar", "EditCompany", new { id=item.Id }) |
            @Html.ActionLink("Eliminar", "DeleteCompany", new { id=item.Id })
        </td>
    </tr>
}

</table>
